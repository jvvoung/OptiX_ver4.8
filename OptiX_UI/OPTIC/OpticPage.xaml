<UserControl x:Class="OptiX.OpticPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    
    <UserControl.Resources>
        <!-- BooleanToVisibilityConverter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- 라이트 모드 색상 팔레트 -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#7C3AED"/>
        <SolidColorBrush x:Key="PrimaryLightColor" Color="#A78BFA"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#10B981"/>
        <SolidColorBrush x:Key="SecondaryDarkColor" Color="#059669"/>
        <SolidColorBrush x:Key="AccentColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="CardColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextPrimaryColor" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#64748B"/>
        <SolidColorBrush x:Key="TextMutedColor" Color="#94A3B8"/>
        
        <!-- 다크 모드 색상 팔레트 -->
        <SolidColorBrush x:Key="DarkBackgroundColor" Color="#0F172A"/>
        <SolidColorBrush x:Key="DarkSurfaceColor" Color="#1E293B"/>
        <SolidColorBrush x:Key="DarkCardColor" Color="#334155"/>
        <SolidColorBrush x:Key="DarkBorderColor" Color="#475569"/>
        <SolidColorBrush x:Key="DarkTextPrimaryColor" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="DarkTextSecondaryColor" Color="#CBD5E1"/>
        <SolidColorBrush x:Key="DarkTextMutedColor" Color="#94A3B8"/>
        
        <!-- 동적 색상 팔레트 (C#에서 변경 가능) -->
        <SolidColorBrush x:Key="DynamicBackgroundColor" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="DynamicSurfaceColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="DynamicCardColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="DynamicBorderColor" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="DynamicTextPrimaryColor" Color="#1E293B"/>
        <SolidColorBrush x:Key="DynamicTextSecondaryColor" Color="#64748B"/>
        <SolidColorBrush x:Key="DynamicTextMutedColor" Color="#94A3B8"/>
        
        <!-- 모던 버튼 스타일 -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="16,12">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" 
                                                Opacity="0.1" 
                                                BlurRadius="8" 
                                                ShadowDepth="2" 
                                                Direction="270"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" 
                                                        Opacity="0.15" 
                                                        BlurRadius="12" 
                                                        ShadowDepth="4" 
                                                        Direction="270"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" 
                                                        Opacity="0.05" 
                                                        BlurRadius="4" 
                                                        ShadowDepth="1" 
                                                        Direction="270"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 세컨더리 버튼 스타일 -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource DynamicSurfaceColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DynamicBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicTextPrimaryColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="16,12">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" 
                                                Opacity="0.05" 
                                                BlurRadius="4" 
                                                ShadowDepth="1" 
                                                Direction="270"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource DynamicBackgroundColor}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" 
                                                        Opacity="0.1" 
                                                        BlurRadius="8" 
                                                        ShadowDepth="2" 
                                                        Direction="270"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource DynamicBackgroundColor}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" 
                                                        Opacity="0.02" 
                                                        BlurRadius="2" 
                                                        ShadowDepth="0" 
                                                        Direction="270"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 탭 버튼 스타일 -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicTextSecondaryColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="16,8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource DynamicBackgroundColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 활성 탭 버튼 스타일 -->
        <Style x:Key="ActiveTabButtonStyle" TargetType="Button" BasedOn="{StaticResource TabButtonStyle}">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Zone 버튼 스타일 -->
        <Style x:Key="ZoneButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource DynamicSurfaceColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DynamicBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicTextSecondaryColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                Padding="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" 
                                                Opacity="0.06" 
                                                BlurRadius="2" 
                                                ShadowDepth="1" 
                                                Direction="270"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="4"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryLightColor}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" 
                                                        Opacity="0.1" 
                                                        BlurRadius="4" 
                                                        ShadowDepth="1" 
                                                        Direction="270"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 활성 Zone 버튼 스타일 -->
        <Style x:Key="ActiveZoneButtonStyle" TargetType="Button" BasedOn="{StaticResource ZoneButtonStyle}">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <!-- DataGrid 행 병합을 위한 스타일 -->
        <Style x:Key="MergedCellStyle" TargetType="DataGridCell">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        
        <!-- DataGrid 헤더 스타일 -->
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource DynamicBackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicTextPrimaryColor}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        
        <!-- DataGrid 셀 스타일 -->
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="{DynamicResource DynamicSurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicTextPrimaryColor}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DynamicBorderColor}"/>
        </Style>
        
        <!-- DataGrid 행 스타일 -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="{DynamicResource DynamicSurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicTextPrimaryColor}"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="{DynamicResource DynamicBackgroundColor}">
        <!-- 뒤로가기 버튼 (직사각형) -->
                <Button x:Name="BackButton" 
                Width="60" 
                        Height="40" 
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="4,4,0,0"
                Style="{StaticResource SecondaryButtonStyle}"
                FontSize="20" 
                        FontWeight="Bold"
                        Command="{Binding BackCommand}" 
>
            <TextBlock Text="←" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       FontSize="20"
                       FontWeight="Bold"/>
        </Button>

        
        
        <!-- 메인 콘텐츠 영역 -->
        <Grid Margin="4,48,4,4">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="280"/>
            </Grid.RowDefinitions>
            
            <!-- 데이터 테이블 카드 (상단) -->
            <Border Grid.Row="0" 
                    Background="{DynamicResource DynamicCardColor}" 
                    BorderBrush="{DynamicResource DynamicBorderColor}" 
                        BorderThickness="1" 
                    CornerRadius="6"
                    Margin="0,0,0,4">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" 
                                    Opacity="0.08" 
                                    BlurRadius="16" 
                                    ShadowDepth="4" 
                                    Direction="270"/>
                </Border.Effect>
                    <Grid>
                        <!-- 테이블 헤더 -->
                    <Border Background="{DynamicResource DynamicBackgroundColor}" 
                            CornerRadius="6,6,0,0" 
                            Height="36"
                            VerticalAlignment="Top">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock x:Name="DataTableTitle"
                                       Grid.Column="0"
                                       Text="📊 특성 데이터 테이블" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                       VerticalAlignment="Center" 
                                       Margin="8,0"/>
                            
                            <!-- WAD 라벨 -->
                            <TextBlock Grid.Column="1"
                                       Text="WAD:"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource DynamicTextColor}"
                                       VerticalAlignment="Center"
                                       Margin="8,0,4,0"/>
                            
                            <!-- WAD 콤보박스 -->
                            <ComboBox Grid.Column="2"
                                      x:Name="WadComboBox"
                                      FontSize="12"
                                      FontWeight="SemiBold"
                                      Background="White"
                                      Foreground="Black"
                                      BorderBrush="{DynamicResource DynamicBorderColor}"
                                      BorderThickness="1"
                                      Padding="8,4"
                                      Margin="0,0,8,0"
                                      Height="28"
                                      VerticalAlignment="Center"
                                      Width="80"
                                      SelectionChanged="WadComboBox_SelectionChanged">
                                <ComboBox.Style>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ComboBox">
                                                    <Grid>
                                                        <ToggleButton x:Name="ToggleButton"
                                                                     Focusable="false"
                                                                     IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                     ClickMode="Press">
                                                            <ToggleButton.Template>
                                                                <ControlTemplate TargetType="ToggleButton">
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                                            CornerRadius="14">
                                                                        <Grid>
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition/>
                                                                                <ColumnDefinition Width="20"/>
                                                                            </Grid.ColumnDefinitions>
                                                                            <ContentPresenter Grid.Column="0"
                                                                                            HorizontalAlignment="Left"
                                                                                            VerticalAlignment="Center"
                                                                                            Margin="8,0,0,0"/>
                                                                            <Path x:Name="Arrow"
                                                                                  Grid.Column="1"
                                                                                  Fill="{DynamicResource DynamicTextColor}"
                                                                                  HorizontalAlignment="Center"
                                                                                  VerticalAlignment="Center"
                                                                                  Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                                        </Grid>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource PrimaryColor}"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </ToggleButton.Template>
                                                        </ToggleButton>
                                                        <ContentPresenter x:Name="ContentSite"
                                                                        IsHitTestVisible="False"
                                                                        Content="{TemplateBinding SelectionBoxItem}"
                                                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                        ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                                        Margin="8,0,23,0"
                                                                        VerticalAlignment="Center"
                                                                        HorizontalAlignment="Left"/>
                                                        <Popup x:Name="Popup"
                                                               Placement="Bottom"
                                                               IsOpen="{TemplateBinding IsDropDownOpen}"
                                                               AllowsTransparency="True"
                                                               Focusable="False"
                                                               PopupAnimation="Slide">
                                                            <Grid x:Name="DropDown"
                                                                  SnapsToDevicePixels="True"
                                                                  MinWidth="{TemplateBinding ActualWidth}"
                                                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                                <Border x:Name="DropDownBorder"
                                                                        Background="White"
                                                                        BorderBrush="{DynamicResource DynamicBorderColor}"
                                                                        BorderThickness="1"
                                                                        CornerRadius="8">
                                                                    <ScrollViewer x:Name="DropDownScrollViewer"
                                                                                  Margin="1"
                                                                                  CanContentScroll="True">
                                                                        <Grid RenderOptions.ClearTypeHint="Enabled">
                                                                            <Canvas HorizontalAlignment="Left"
                                                                                    Height="0"
                                                                                    Width="0">
                                                                                <Rectangle x:Name="OpaqueRect"
                                                                                          Fill="White"
                                                                                          Height="{Binding ActualHeight, ElementName=DropDownBorder}"
                                                                                          Width="{Binding ActualWidth, ElementName=DropDownBorder}"/>
                                                                            </Canvas>
                                                                            <ItemsPresenter x:Name="ItemsPresenter"
                                                                                           KeyboardNavigation.DirectionalNavigation="Contained"
                                                                                           SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                                        </Grid>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </Popup>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="ToggleButton" Property="Background" Value="{DynamicResource DynamicInputBackground}"/>
                                                            <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.Style>
                            </ComboBox>
                            
                            <!-- RESET 버튼 -->
                            <Button Grid.Column="3"
                                    x:Name="ResetButton"
                                    Content="🔄 RESET"
                                    FontSize="11"
                                    FontWeight="SemiBold"
                                    Background="{DynamicResource DynamicSurfaceColor}"
                                    Foreground="{DynamicResource DynamicTextColor}"
                                    BorderBrush="{DynamicResource DynamicBorderColor}"
                                    BorderThickness="1"
                                    Padding="8,4"
                                    Margin="4,0,8,0"
                                    Height="28"
                                    VerticalAlignment="Center"
                                    Width="70"
                                    Click="ResetButton_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="14">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"
                                                                        Margin="{TemplateBinding Padding}"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource DynamicInputBackground}"/>
                                                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                        </Border>
                        
                        
                    <!-- 스크롤 가능한 테이블 영역 -->
                    <ScrollViewer x:Name="MainScrollViewer" 
                                  HorizontalScrollBarVisibility="Auto" 
                                  VerticalScrollBarVisibility="Auto"
                                  CanContentScroll="True"
                                  Margin="0,36,0,0">
                        <Grid x:Name="DataTableGrid" Background="{DynamicResource DynamicSurfaceColor}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ScrollViewer>
                    </Grid>
                </Border>
            
            <!-- 하단 영역 (Graph/Total + 특성 판정 현황 + Control Panel) -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Graph/Total 영역 카드 (하단, 왼쪽) -->
                <Border Grid.Column="0" 
                        Background="{DynamicResource DynamicCardColor}" 
                        BorderBrush="{DynamicResource DynamicBorderColor}" 
                        BorderThickness="1"
                        CornerRadius="6"
                        Margin="0,0,2,0">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" 
                                        Opacity="0.08" 
                                        BlurRadius="16" 
                                        ShadowDepth="4" 
                                        Direction="270"/>
                    </Border.Effect>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
                        <!-- 탭 버튼들 (영역 위쪽) -->
                        <Border Grid.Row="0" 
                                Background="{DynamicResource DynamicBackgroundColor}" 
                                CornerRadius="8,8,0,0" 
                                Height="40">
                            <StackPanel Orientation="Horizontal" 
                                        VerticalAlignment="Center" 
                                        Margin="12,0">
                                <Button x:Name="MonitorTab" 
                                        Content="🖥 Monitor" 
                                        Style="{StaticResource ActiveTabButtonStyle}"
                                        FontSize="13"
                                        Margin="0,0,4,0"
                                        Click="MonitorTab_Click"/>
                                <Button x:Name="GraphTab" 
                                        Content="📊 Graph" 
                                        Style="{StaticResource TabButtonStyle}"
                                        FontSize="13" 
                                        Margin="4,0,4,0"
                                        Click="GraphTab_Click"
/>
                                <Button x:Name="TotalTab" 
                                        Content="📈 Total" 
                                        Style="{StaticResource TabButtonStyle}"
                                        FontSize="13"
                                        Click="TotalTab_Click"
/>
                        </StackPanel>
                    </Border>
                    
                        <!-- 콘텐츠 영역 -->
                <Border Grid.Row="1" 
                                Background="{DynamicResource DynamicSurfaceColor}" 
                                CornerRadius="0,0,8,8">
                            <Grid Margin="12">
                                <Grid x:Name="MonitorContent" Visibility="Visible">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                        <Grid x:Name="MonitorGrid" />
                                    </ScrollViewer>
                                </Grid>
                                <ScrollViewer x:Name="GraphScrollViewer" 
                                              Visibility="Collapsed"
                                              VerticalScrollBarVisibility="Auto" 
                                              HorizontalScrollBarVisibility="Disabled">
                                    <Grid x:Name="GraphContent">
                                        <!-- 동적으로 생성될 그래프 영역 -->
                                    </Grid>
                                </ScrollViewer>
                                <Grid x:Name="TotalContent" Visibility="Collapsed">
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock Text="📈" 
                                                   FontSize="40" 
                                                   HorizontalAlignment="Center" 
                                                   Margin="0,0,0,12"/>
                                        <TextBlock Text="Total 영역" 
                                                   FontSize="16" 
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                   HorizontalAlignment="Center" 
                                                   Margin="0,0,0,6"/>
                                        <TextBlock Text="전체 데이터 요약 정보가 표시됩니다" 
                                                   FontSize="13" 
                                                   Foreground="{DynamicResource DynamicTextSecondaryColor}"
                                                   HorizontalAlignment="Center"/>
            </StackPanel>
                                    </Grid>
                                    </Grid>
                                </Border>
                    </Grid>
                    </Border>
                    
                <!-- 특성 판정 현황 카드 (가운데) -->
                <Border Grid.Column="1" 
                        Background="{DynamicResource DynamicCardColor}" 
                        BorderBrush="{DynamicResource DynamicBorderColor}" 
                        BorderThickness="1" 
                        CornerRadius="6"
                        Margin="4,0,4,0">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" 
                                        Opacity="0.08" 
                                        BlurRadius="16" 
                                        ShadowDepth="4" 
                                        Direction="270"/>
                    </Border.Effect>
                    <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                        <!-- 헤더 -->
                        <Border Grid.Row="0" 
                                Background="{DynamicResource DynamicBackgroundColor}" 
                                CornerRadius="6,6,0,0"
                                Height="36">
                            <TextBlock x:Name="JudgmentStatusTitle"
                                       Text="📊 특성 판정 현황" 
                                       FontSize="13" 
                          FontWeight="Bold" 
                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                       VerticalAlignment="Center" 
                                       Margin="8,0"/>
                        </Border>
                        
                        <!-- 콘텐츠 영역 -->
                <Border Grid.Row="1" 
                                Background="{DynamicResource DynamicSurfaceColor}" 
                                CornerRadius="0,0,6,6">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                                <!-- 표 헤더 -->
                        <Border Grid.Row="0" 
                                        Background="{DynamicResource DynamicBackgroundColor}" 
                                        CornerRadius="6,6,0,0" 
                                        Height="36"
                                        VerticalAlignment="Top">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" 
                                                   Text="" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"/>
                                        <TextBlock x:Name="QuantityHeader"
                                                   Grid.Column="1" 
                                                   Text="수량" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"/>
                                        <TextBlock x:Name="OccurrenceRateHeader"
                                                   Grid.Column="2" 
                                                   Text="발생률" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        
                                <!-- 표 데이터 -->
                                <Grid Grid.Row="1" 
                                      Background="{DynamicResource DynamicSurfaceColor}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Total 행 -->
                                    <Border Grid.Row="0" 
                                            Background="{DynamicResource DynamicSurfaceColor}" 
                                            BorderBrush="{DynamicResource DynamicBorderColor}" 
                                            BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" 
                                                       Text="Total" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                            <TextBlock x:Name="TotalQuantity"
                                                       Grid.Column="1" 
                                                       Text="0" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                            <TextBlock x:Name="TotalRate"
                                                       Grid.Column="2" 
                                                       Text="0.00" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                    </Grid>
                                </Border>
                                
                                    <!-- PTN 행 -->
                                    <Border Grid.Row="1" 
                                            Background="{DynamicResource DynamicSurfaceColor}" 
                                            BorderBrush="{DynamicResource DynamicBorderColor}" 
                                            BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" 
                                                       Text="PTN" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                            <TextBlock x:Name="PTNQuantity"
                                                       Grid.Column="1" 
                                                       Text="0" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                            <TextBlock x:Name="PTNRate"
                                                       Grid.Column="2" 
                                                       Text="0.00" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                    </Grid>
                                </Border>
                                
                                    <!-- R/J 행 -->
                                    <Border Grid.Row="2" 
                                            Background="{DynamicResource DynamicSurfaceColor}" 
                                            BorderBrush="{DynamicResource DynamicBorderColor}" 
                                            BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" 
                                                       Text="R/J" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                            <TextBlock x:Name="RJQuantity"
                                                       Grid.Column="1" 
                                                       Text="0" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                            <TextBlock x:Name="RJRate"
                                                       Grid.Column="2" 
                                                       Text="0.00" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                    </Grid>
                                </Border>
                                
                                    <!-- OK 행 -->
                                    <Border Grid.Row="3" 
                                            Background="{DynamicResource DynamicSurfaceColor}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" 
                                                       Text="OK" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                            <TextBlock x:Name="OKQuantity"
                                                       Grid.Column="1" 
                                                       Text="0" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                            <TextBlock x:Name="OKRate"
                                                       Grid.Column="2" 
                                                       Text="0.00" 
                                                       FontSize="14" 
                                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                    </Grid>
                                </Border>
                                    </Grid>
                    </Grid>
                </Border>
            </Grid>
                </Border>
                
                <!-- 컨트롤 패널 카드 (오른쪽) -->
                <Border Grid.Column="2" 
                        Background="{DynamicResource DynamicCardColor}" 
                        BorderBrush="{DynamicResource DynamicBorderColor}" 
                            BorderThickness="1" 
                        CornerRadius="6"
                        Margin="2,0,0,0">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" 
                                        Opacity="0.08" 
                                        BlurRadius="16" 
                                        ShadowDepth="4" 
                                        Direction="270"/>
                    </Border.Effect>
                    <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                        <!-- 컨트롤 패널 헤더 -->
                        <Border Grid.Row="0" 
                                Background="{DynamicResource DynamicBackgroundColor}" 
                                CornerRadius="6,6,0,0" 
                                Height="36">
                            <TextBlock x:Name="ControlPanelTitle"
                                       Text="⚙️ 컨트롤 패널" 
                                       FontSize="13" 
                                       FontWeight="SemiBold" 
                                       Foreground="{DynamicResource DynamicTextPrimaryColor}"
                                       VerticalAlignment="Center" 
                                       Margin="8,0"/>
                    </Border>
                    
                        <!-- 컨트롤 버튼들 - 동적 크기 조절 -->
                        <Grid Grid.Row="1" Margin="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Set Params, Set Path 버튼 -->
                            <Grid Grid.Row="0" Margin="0,4,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0"
                                        x:Name="SettingButton"
                                        Content="⚙️ Setting"
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        FontSize="10"
                                        Height="50"
                                        Margin="0,0,4,0"
                                        Command="{Binding SettingCommand}"
/>
                                <Button Grid.Column="1"
                                        x:Name="SetPathButton"
                                        Content="📁 Path" 
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        FontSize="10"
                                        Height="50"
                                        Margin="4,0,0,0"
                                        Command="{Binding PathCommand}"
/>
                            </Grid>
                            
                            <!-- Zone 선택 버튼들 - Test Start 바로 위에 배치 -->
                            <StackPanel x:Name="ZoneButtonsPanel" 
                                        Grid.Row="1"
                                        Orientation="Horizontal" 
                                        HorizontalAlignment="Center"
                                        Margin="0,4,0,4">
                        </StackPanel>
                            
                            <!-- Test Start 버튼 - 남은 공간을 모두 사용 -->
                            <Button Grid.Row="2"
                                    x:Name="TestStartButton"
                                    Content="🚀 Test Start!!" 
                                    Style="{StaticResource ModernButtonStyle}"
                                    FontSize="20"
                                    Height="60"
                                    VerticalAlignment="Center"
                                    Margin="0,4,0,4"
                                    Command="{Binding TestStartCommand}"/>
                        </Grid>
                    </Grid>
                    </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
