<Window x:Class="OptiX.Common.CellIdInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zone 1 OPTIC SETTING"
        Height="750"
        Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="600"
        MinWidth="600"
        Background="#F8FAFC">
    
    <Window.Resources>
        <!-- 보라색 컬러 팔레트 (PATH UI와 완전 동일) -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#7C3AED"/>
        <SolidColorBrush x:Key="PrimaryLightColor" Color="#A78BFA"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#10B981"/>
        <SolidColorBrush x:Key="SecondaryDarkColor" Color="#059669"/>
        <SolidColorBrush x:Key="AccentColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#F8FAFC"/>
        
        <!-- 동적 색상 팔레트 -->
        <SolidColorBrush x:Key="DynamicWindowBackground" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="DynamicCardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="DynamicBorderColor" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="DynamicTextColor" Color="#1E293B"/>
        <SolidColorBrush x:Key="DynamicTextSecondary" Color="#64748B"/>
        <SolidColorBrush x:Key="DynamicInputBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="DynamicInputBorder" Color="#D1D5DB"/>
        <SolidColorBrush x:Key="DynamicSecondaryButtonBackground" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="DynamicSecondaryButtonText" Color="#374151"/>
        
        <!-- 모던 버튼 스타일 -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#8B5CF6" 
                                                        BlurRadius="10" 
                                                        ShadowDepth="0" 
                                                        Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 세컨더리 버튼 스타일 -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource DynamicSecondaryButtonBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DynamicBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicSecondaryButtonText}"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="4" ShadowDepth="1" Direction="270"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource DynamicInputBackground}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="8" ShadowDepth="2" Direction="270"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource DynamicBorderColor}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" Opacity="0.03" BlurRadius="2" ShadowDepth="0" Direction="270"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 모던 TextBox 스타일 -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource DynamicInputBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DynamicInputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicTextColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SelectionBrush" Value="{StaticResource PrimaryLightColor}"/>
            <Setter Property="SelectionTextBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 포트 TextBox 스타일 -->
        <Style x:Key="PortTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource DynamicInputBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DynamicInputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource DynamicTextColor}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Background="{DynamicResource DynamicWindowBackground}">
        <Border Background="{DynamicResource DynamicCardBackground}"
                BorderBrush="{DynamicResource DynamicBorderColor}"
                BorderThickness="1"
                CornerRadius="12"
                Margin="20"
                Padding="0">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="20" ShadowDepth="8" Direction="270"/>
            </Border.Effect>
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Cell 정보 영역 -->
                <Border Grid.Row="0" 
                        Background="{DynamicResource DynamicCardBackground}"
                        BorderBrush="{DynamicResource DynamicBorderColor}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock x:Name="CellInfoTitle"
                                   Text="Cell 정보" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="{DynamicResource DynamicTextColor}" 
                                   Margin="0,0,0,12"/>
                        <ScrollViewer x:Name="CellInfoScrollViewer"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Background="{DynamicResource DynamicCardBackground}"
                                      BorderBrush="{DynamicResource DynamicBorderColor}"
                                      BorderThickness="1"
                                      Padding="8"
                                      MaxHeight="220">
                            <StackPanel x:Name="CellInfoStackPanel"
                                        Orientation="Vertical"
                                        Background="{DynamicResource DynamicCardBackground}"/>
                        </ScrollViewer>
                    </StackPanel>
                </Border>


                <!-- Port 연결 영역 -->
                <Border Grid.Row="1" 
                        Background="{DynamicResource DynamicCardBackground}"
                        BorderBrush="{DynamicResource DynamicBorderColor}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Port 연결 제목과 All Connect 버튼 -->
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock x:Name="PortConnectionTitle"
                                       Grid.Column="0" 
                                       Text="Port 연결" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" 
                                       Foreground="{DynamicResource DynamicTextColor}" 
                                       VerticalAlignment="Center"/>
                            
                            <Button Grid.Column="1"
                                    x:Name="AllConnectButton"
                                    Content="ALL CONNECT"
                                    Style="{StaticResource ModernButtonStyle}"
                                    FontSize="11"
                                    Height="30"
                                    Width="120"
                                    Padding="10,2"
                                    Click="AllConnectButton_Click"/>
                        </Grid>
                        
                        <!-- Port 섹션 (스크롤 가능) -->
                        <ScrollViewer Grid.Row="1" 
                                      HorizontalScrollBarVisibility="Disabled" 
                                      VerticalScrollBarVisibility="Auto"
                                      Background="{DynamicResource DynamicCardBackground}"
                                      BorderBrush="{DynamicResource DynamicBorderColor}"
                                      BorderThickness="1"
                                      Padding="8">
                            <StackPanel x:Name="PortSectionsStackPanel"
                                        Background="{DynamicResource DynamicCardBackground}"
                                        Orientation="Vertical"
                                        Margin="0"/>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- 버튼들 -->
                <StackPanel Grid.Row="2" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            Margin="0,16,0,0">
                    <Button x:Name="SaveButton"
                            Content="💾 SAVE" 
                            Style="{StaticResource ModernButtonStyle}"
                            FontSize="14"
                            Height="40"
                            Width="120"
                            Margin="0,0,8,0"
                            Click="SaveButton_Click"/>
                    <Button x:Name="CancelButton"
                            Content="CANCEL" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            FontSize="12"
                            Height="40"
                            Width="110"
                            Click="CancelButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>